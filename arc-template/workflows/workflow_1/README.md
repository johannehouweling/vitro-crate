# workflow_1 — example, replace me

A reusable computational tool/script (e.g. an R or Python analysis) plus the
environment it needs (dependencies + versions). Each *execution* gets its own
folder under ../../runs/.
